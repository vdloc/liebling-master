{{#get
  "posts"
  filter="featured:false+tag:{{postsTag}}"
  include="authors,tags"
  limit=limit
  as |nonFeaturedPosts|
}}

  {{#if nonFeaturedPosts}}
    {{#foreach nonFeaturedPosts}}
      {{> common/post-card index=@index}}
    {{/foreach}}
  {{else}}
  {{/if}}
{{/get}}